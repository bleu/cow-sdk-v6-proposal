{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test --no-cache",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test:ci": "jest --ci --json --coverage --testLocationInResults --outputFile=test-results/jest.results.json"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.3"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-rollup",
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
