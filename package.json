{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "jest",
    "test:ci": "jest --ci --coverage --json --outputFile=test-results/jest.results.json",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.3",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-rollup",
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
